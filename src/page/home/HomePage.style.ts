import styled from "styled-components";

const S = {
    container: styled.div`
        display: flex;
        overflow-y: scroll;
        align-items: stretch;
        flex-direction: column;
    `
};

export default S;
import styled from "styled-components";

const S = {
    container: styled.div`
        display: flex;
        width: 100vw;
        flex: 1;
        background: var(--g-200);
        overflow: hidden;
    `,
};

export default S;